{
  "name": "reto_pichincha",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "playwright test",
    "test:report": "playwright test --reporter=html && playwright show-report",
    "allure:reportlite": "allure generate --clean --single-file allure-results",
    "allure:report": "allure generate --clean --single-file allure-results -o allure-report && allure open allure-report"

  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@faker-js/faker": "^10.0.0",
    "@playwright/test": "^1.55.0",
    "@types/node": "^24.3.0",
    "allure-playwright": "^3.3.3",
    "dotenv": "^17.2.2"
  }
}